= simple_form_for @intersection, :html => { :class => "form-horizontal" } do |f|
  = f.input :name
  = error_span(@intersection[:name])
  = f.button :submit, :class => 'btn-primary'
  '
  = link_to t('.cancel', :default => t("helpers.links.cancel")), intersections_path, :class => 'btn btn-default'
