= simple_form_for @insurance, :html => { :class => "form-horizontal" } do |f|
  = f.input :name
  = error_span(@insurance[:name])
  = f.button :submit, :class => 'btn-primary'
  '
  = link_to t('.cancel', :default => t("helpers.links.cancel")), insurances_path, :class => 'btn btn-default'
