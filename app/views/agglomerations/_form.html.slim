= simple_form_for @agglomeration, :html => { :class => "form-horizontal" } do |f|
  = f.input :name
  = error_span(@agglomeration[:name])
  = f.button :submit, :class => 'btn-primary'
  '
  = link_to t('.cancel', :default => t("helpers.links.cancel")), agglomerations_path, :class => 'btn btn-default'
