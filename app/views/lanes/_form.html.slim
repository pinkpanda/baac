= simple_form_for @lane, :html => { :class => "form-horizontal" } do |f|
  = f.input :name
  = error_span(@lane[:name])
  = f.button :submit, :class => 'btn-primary'
  '
  = link_to t('.cancel', :default => t("helpers.links.cancel")), lanes_path, :class => 'btn btn-default'
