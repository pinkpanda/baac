= simple_form_for @zone, :html => { :class => "form-horizontal" } do |f|
  = f.input :name
  = error_span(@zone[:name])
  = f.button :submit, :class => 'btn-primary'
  '
  = link_to t('.cancel', :default => t("helpers.links.cancel")), zones_path, :class => 'btn btn-default'
