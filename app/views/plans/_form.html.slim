= simple_form_for @plan, :html => { :class => "form-horizontal" } do |f|
  = f.input :name
  = error_span(@plan[:name])
  = f.button :submit, :class => 'btn-primary'
  '
  = link_to t('.cancel', :default => t("helpers.links.cancel")), plans_path, :class => 'btn btn-default'
