= simple_form_for @traffic, :html => { :class => "form-horizontal" } do |f|
  = f.input :name
  = error_span(@traffic[:name])
  = f.button :submit, :class => 'btn-primary'
  '
  = link_to t('.cancel', :default => t("helpers.links.cancel")), traffics_path, :class => 'btn btn-default'
