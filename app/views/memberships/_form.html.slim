= simple_form_for @membership, :html => { :class => "form-horizontal" } do |f|
  = f.input :name
  = error_span(@membership[:name])
  = f.button :submit, :class => 'btn-primary'
  '
  = link_to t('.cancel', :default => t("helpers.links.cancel")), memberships_path, :class => 'btn btn-default'
