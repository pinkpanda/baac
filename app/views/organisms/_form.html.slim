= simple_form_for @organism, :html => { :class => "form-horizontal" } do |f|
  = f.input :name
  = error_span(@organism[:name])
  = f.button :submit, :class => 'btn-primary'
  '
  = link_to t('.cancel', :default => t("helpers.links.cancel")), organisms_path, :class => 'btn btn-default'
